{
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.24.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply copilot-db",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/tokenizer": "^0.0.4",
    "gpt-tokenizer": "^3.4.0",
    "hono": "^4"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "4.0.18",
    "wrangler": "^4.81.0"
  }
}
